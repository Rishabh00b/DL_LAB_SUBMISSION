import numpy as np
a=np.array([1,2])
print(a)
b=np.array([1,2])

print(b)
print("weighted sum:",np.dot(a, b))


# import numpy as np

w = np.array([0.2, 0.4])
x = np.array([1.0, 2.0])

print("Weighted sum:", np.dot(w, x))